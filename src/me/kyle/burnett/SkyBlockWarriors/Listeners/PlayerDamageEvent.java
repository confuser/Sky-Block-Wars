package me.kyle.burnett.SkyBlockWarriors.Listeners;

import org.bukkit.event.Listener;

public class PlayerDamageEvent implements Listener {


    /*
     * @EventHandler public void playerDamage(EntityDamageByEntityEvent e) {
     * Entity ent = e.getEntity();
     * 
     * if (ent instanceof Player) {
     * 
     * GameManager gm = GameManager.getInstance(); Player p = (Player) ent;
     * 
     * if (gm.isPlayerInGame(p)) {
     * 
     * if (gm.hasPlayerGameStarted(p)) {
     * 
     * Entity ed = e.getDamager();
     * 
     * if (ed instanceof Player) {
     * 
     * Player pd = (Player) ed; // Team team =
     * gm.getPlayerGame(p).getPlayerTeam(p); // if
     * (team.getPlayers().contains(
     * Bukkit.getServer().getOfflinePlayer(pd.getName()))) { //
     * e.setCancelled(true); // } } }
     * 
     * }
     * 
     * 
     * } }
     */

}
